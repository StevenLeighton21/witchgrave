<%= form_for(@campaigns) do |f| %>
  <% if @campaigns.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaigns.errors.count, "error") %> prohibited this campaigns from being saved:</h2>

      <ul>
      <% @campaigns.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.label :name %>
      <%= f.text_field :name %>
</div>
  <div class="field">
      <%= f.label :description %>
      <%= f.text_field :description %>
</div>

 <div class="actions">
      <%= f.submit "Create campaigns", class: "btn btn-primary" %>
  </div>


<% end %>
